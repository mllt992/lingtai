# 回归用例 · 启动路径双写与回退

- **关联方案**：`docs/02_功能设计/启动路径双写与回退_优化方案_20260917.md`
- **日期**：2026-09-17
- **自动化**：Rust `cargo test --lib path_resolve`（8 项）已通过；下列为手工/UI 回归

| 用例 | 步骤 | 期望 | 优先级 |
|---|---|---|---|
| G-01 无根旧行为 | 不配根，加载旧 `items.json` 并启动 | 与改前一致，无弹窗 | P0 |
| G-02 根下新增 | 配置根后拖入根下 exe/文件 | `items.json` 含 `relPath`/`rootHint` | P0 |
| G-03 非根新增 | 拖入根外路径 | `relPath=null`，可正常启动/打开 | P0 |
| G-04 相对回退询问 | 将绝对路径改坏、保留 relPath、配好根，点击启动 | 弹「修复路径」对话框；「修复并打开」后 path 更新且启动成功 | P0 |
| G-05 仅本次打开 | 同 G-04，选「仅本次打开」 | 能打开，path 不被写回 | P1 |
| G-06 取消 | 同 G-04，选取消 | 不打开、不写 path | P1 |
| G-07 自动修复 | 开启自动修复后重复 G-04 | 无阻塞弹窗，path 更新并打开 | P0 |
| G-08 双失效 | path 与 rel 均无效 | 报错提示，不崩溃 | P0 |
| G-09 url | 网址资源加载/打开 | 无检测角标，打开正常 | P0 |
| G-10 lnk 修复 | .lnk 的 path 失效、相对可恢复 | 修复后 target/iconPath 重解析；iconData 不被错误覆盖（图标可仍显示旧缓存） | P1 |
| G-11 回填 | 存量绝对路径在根下，点「回填相对路径」 | 提示回填条数；字段写入 | P1 |
| G-12 设置持久化 | 配置根与开关后重启应用 | 配置仍在 | P0 |
| G-13 健康角标 | 坏路径条目进入启动器/资源页 | 红/黄角标；右键「修复路径」仅在可恢复时可用 | P1 |
| G-14 多根最长匹配 | 两层根 `D:\Portable` 与 `D:\Portable\Apps`，路径在 Apps 下 | rel 相对更长的 Apps 根 | P2 |

## 自动化结果（2026-09-17）

```
cargo test --lib path_resolve
test result: ok. 8 passed; 0 failed
```

覆盖：最长根匹配、根外 null、拒绝 `..`、大小写/斜杠、URL 跳过、绝对优先、相对回退、check 状态。
