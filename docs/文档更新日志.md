# 文档更新日志

| 日期 | 变更 | 说明 |
|---|---|---|
| 2026-09-16 | 新增环境变量板块 | 需求分析、实施方案、用户手册、回归用例 |
| 2026-09-17 | 修复启动白屏 | 根因：scoped CSS `:global()` 选择器写法错误导致 `html{display:none}`；修复 7 个文件 39 处；补回归用例 |
| 2026-09-17 | 修复性能监控卡死 | 根因：每秒 spawn PowerShell 查 WMI 并持锁；改为 EnumDisplayDevices + GPU 静态缓存 + 放锁采样 + 前端防重入；补回归用例 |
| 2026-09-17 | 启动路径双写与回退 | 配置路径根目录；启动项/资源写 relPath/rootHint；绝对失效时相对回退；询问/自动修复；列表健康角标；只修主路径不改 iconData |
